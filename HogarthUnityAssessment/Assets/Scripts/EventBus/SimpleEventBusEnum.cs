using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SimpleEventBus_eon {
    public class SimpleEventBusEnum {
        public enum UI_EVENTS {
           ON_WIN,
        }
        public enum GAME_EVENTS {
            ON_PLAYER_DIED,
        }
    }
}